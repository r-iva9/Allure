[package]
name = "r-iva9/allure"
description = "Node Actor Framework for Luau"
license = "MIT"
authors = ["r-iva9"]
version = "0.1.0"

registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"

exclude = ["**"]
include = ["src", "src/*", "default.project.json", "wally.lock", "wally.toml"]

[dependencies]
